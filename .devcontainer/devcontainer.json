{
    "waitFor": "onCreateCommand",
    "hostRequirements": {
      "cpus": 4
    },
    "onCreateCommand": "pipx install sqlite-utils && pipx install datasette && datasette install datasette-dashboards datasette-codespaces",
    "customizations": {
      "vscode": {
        "extensions": [
          "rioj7.command-variable"
        ]
      },
      "codespaces": {
        "openFiles": [
          "metadata.yml"
        ]
      }
  }
}
